# workspace/auth/

**Trust boundary simulado**: login y MFA del prototipo.

- Se construye en: **PM-UX14-03**
- No hay IdP real, sesión real ni MFA real (ver DC-19 en `PLAN.md`)

Punto de entrada del recorrido: Landing → Start Adoption (`adopcion.html`) →
Auth simulado → MFA simulado → `workspace/`.

Vacío por diseño en PM-UX14-01: el scaffold sólo reserva la ruta.
